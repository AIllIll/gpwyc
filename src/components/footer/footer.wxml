<!--
  此文件是由模板文件 ".dtpl/component/$rawModuleName.wxml.dtpl" 生成的，你可以自行修改模板

  注意：
  1. 如果没有语法高亮，且你使用的是 vscode 编辑器的话，建义在 vscode 插件库中安装 minapp-vscode 插件
-->

<view class="footer footer_fixed-bottom">
    <view class="footer__links">
        <navigator url="{{linkUrl}}" class="footer__link">{{linkName}}</navigator>
    </view>
    
    <block wx:for="{{textArray}}">
        <view class="footer__text">{{item}}</view>
    </block>
</view>