<!--
  此文件是由模板文件 ".dtpl/page/$rawModuleName.wxml.dtpl" 生成的，你可以自行修改模板

  注意：
  1. 如果没有语法高亮，且你使用的是 vscode 编辑器的话，建义在 vscode 插件库中安装 minapp-vscode 插件
-->
<!--
<button type="primary" bind:tap="getQR">获取</button>
<image style="height:60px;width:60px;" wx:if="{{QRcode}}" src="{{QRcode}}" />
<image style="height:60px;width:60px;" wx:else src="{{userInfo.avatarUrl}}" />
<image style="height:60px;width:60px;"  src="https://ttissoft.cn/testQR" />
-->
<button type="warn" bind:tap="deleteUser">删除user表</button>
<button type="warn" bind:tap="deleteTask">删除task表</button>
<button type="warn" bind:tap="deleteNotice">删除notice表</button>

<button type="primary" bind:tap="test">发送</button>

<button type="primary" bind:tap="connectSocket1">新建socket</button>
<button type="primary" bind:tap="connectSocket2">新建socket2</button>
<button type="primary" bind:tap="closeSocket1">断开socket1</button>
<button type="primary" bind:tap="test2">2发送到1</button>

<button type="primary" bind:tap="test3">notice测试</button>


